[REPOSITORY]
org = radon
repository = radon-functions

[FUNCTION]
name = aws_keycheck
version = 0.0.1
description = Checks the rights of a given aws key pair. Takes AWS keypair as JSON data

[RUNTIME]
provider = aws
runtime = python3.8
handler = handler
