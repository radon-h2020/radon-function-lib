[REPOSITORY]
org = radon
repository = radon-functions

[FUNCTION]
name = snyk_test_node
version = 0.0.1
description = Uses Snyk to test the dependency files of cloudstash.io function artifacts. Expects JSON post with data: <artificat_id> or <artifact_url>

[RUNTIME]
provider = aws
runtime = python3.8
handler = handler
